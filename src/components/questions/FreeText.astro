---
interface Props {
  name: string;
  value?: string;
  placeholder?: string;
  rows?: number;
  labelId?: string;
  required?: boolean;
  inputFocusClass?: string;
}

const { name, value, placeholder = 'Your answer…', rows = 4, labelId, required = false, inputFocusClass = 'focus:border-violet-500/60 focus:ring-violet-500/30' } = Astro.props;
---

<textarea
  name={name}
  rows={rows}
  placeholder={placeholder}
  aria-labelledby={labelId}
  aria-required={required ? 'true' : undefined}
  class:list={["w-full resize-y rounded-xl border border-white/15 bg-white/8 px-4 py-2.5 text-sm text-white shadow-sm backdrop-blur-sm placeholder:text-white/30 transition-colors focus:outline-none focus:ring-2", inputFocusClass]}
>{value ?? ''}</textarea>
